<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ihre Leistungsbeschreibung - Lehner Haus</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="header-inner">
                <div class="logo">
                    <h1>LEHNER HAUS</h1>
                    <p class="tagline">schwabisch gut</p>
                </div>
            </div>
        </header>

        <main class="main-content result-page">
            <div class="result-header">
                <h2>Ihre Leistungsbeschreibung</h2>
                <p><%= submission.bauherr_anrede %> <%= submission.bauherr_vorname %> <%= submission.bauherr_nachname %></p>
            </div>

            <div class="result-meta">
                <span>Energiestandard: <strong><%= submission.kfw_standard === 'KFW55' ? 'KfW 55' : 'KfW 40' %></strong></span>
                <span>Personen: <strong><%= submission.personenanzahl %></strong></span>
                <% if (submission.grundstueck) { %>
                <span>Grundstück: <strong><%= submission.grundstueck === 'vorhanden' ? 'Vorhanden' : submission.grundstueck === 'in_aussicht' ? 'In Aussicht' : 'Auf der Suche' %></strong></span>
                <% } %>
            </div>

            <% if (components.length > 0) { %>
            <div class="result-components">
                <% components.forEach(comp => { %>
                <div class="result-component">
                    <% if (comp.filePath) { %>
                    <div class="result-component-image">
                        <img src="/<%= comp.filePath %>" alt="<%= comp.name %>">
                    </div>
                    <% } %>
                    <div class="result-component-info">
                        <span class="result-component-label"><%= comp.label %></span>
                        <strong class="result-component-name"><%= comp.name %></strong>
                        <% if (comp.description) { %>
                        <p class="result-component-desc"><%= comp.description %></p>
                        <% } %>
                    </div>
                </div>
                <% }); %>
            </div>
            <% } %>

            <div class="result-actions">
                <a href="/pdf/<%= submission.id %>" class="btn-submit" target="_blank">PDF ansehen</a>
                <a href="/pdf/<%= submission.id %>?download=1" class="btn-secondary-action">PDF herunterladen</a>
            </div>

            <div class="result-footer-links">
                <a href="/">Neue Konfiguration</a>
                <a href="javascript:history.back()">Konfiguration bearbeiten</a>
            </div>
        </main>

        <footer class="footer">
            <p>&copy; 2026 Lehner Haus GmbH & Co. KG</p>
        </footer>
    </div>
</body>
</html>
